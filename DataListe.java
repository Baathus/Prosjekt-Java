import java.util.*;


public class DataListe {
	Map<Calendar,Data> m;
	Set<Calendar> nøkler = m.keySet();
	
	Iterator<Calendar> iter = nøkler.iterator();
	
	while( iter.hasNext() )
	{
		Calendar nøkkel = iter.next();
	}
}
